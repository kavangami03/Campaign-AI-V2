# Logos

`CampaignX` renders its own mark from `src/components/ui/Logo.tsx` (inline SVG,
inherits `--foreground`, no network request).

Partner or customer logos are deliberately absent — none have been verified.
Add SVGs here as `partner-<name>.svg` once permission exists.
